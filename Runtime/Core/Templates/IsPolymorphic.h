#pragma once

/*///////////////////////////////////////////////////////////////////////////////////////////////*/
// TIsPolymorphic

template<typename T>
struct TIsPolymorphic
{
    enum
    {
        Value = __is_polymorphic(T)
    };
};