#pragma once

/*///////////////////////////////////////////////////////////////////////////////////////////////*/
// TIsUnion

template<typename T>
struct TIsUnion
{
    enum { Value = __is_union(T) };
};